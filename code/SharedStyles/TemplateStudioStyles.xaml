<ResourceDictionary
    x:Class="Microsoft.Templates.UI.Styles.ListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Themes="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
    xmlns:catalog="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.ImageCatalog"
    xmlns:controls="clr-namespace:Microsoft.Templates.UI.Controls"
    xmlns:converters="clr-namespace:Microsoft.Templates.UI.Converters"
    xmlns:extensions="clr-namespace:Microsoft.Templates.UI.Extensions"
    xmlns:imaging="clr-namespace:Microsoft.VisualStudio.Imaging;assembly=Microsoft.VisualStudio.Imaging"
    xmlns:res="clr-namespace:Microsoft.Templates.SharedResources;assembly=SharedResources"
    xmlns:services="clr-namespace:Microsoft.Templates.UI.Services"
    xmlns:templateSelector="clr-namespace:Microsoft.Templates.UI.TemplateSelectors"
    xmlns:theming="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Imaging"
    xmlns:utilities="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Utilities">

    <!--
        This file groups all resources into a single file for ease of cross referencing.
        This makes things MUCH easier when used from a SharedProject
    -->

    <!--#region DrawingImage-->

    <DrawingImage x:Key="TSIconWarning">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <DrawingGroup.Children>
                    <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
                    <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M7.0002,-0.000199999999999534L0.000200000000000422,13.9998 2.0002,15.9998 14.0002,15.9998 16.0002,13.9998 9.0002,-0.000199999999999534z" />
                    <GeometryDrawing Brush="#FFFFCC00" Geometry="F1M9,10L7,10 7,5 9,5z M9,13L7,13 7,11 9,11z M8.382,1L7.618,1 1.217,13.803 2.5,15 13.5,15 14.783,13.803z" />
                    <GeometryDrawing Brush="#FF000000" Geometry="F1M9,11L7,11 7,13 9,13z M9,10L7,10 7,5 9,5z" />
                </DrawingGroup.Children>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <DrawingImage x:Key="TSIconError">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <DrawingGroup.Children>
                    <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
                    <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M16,8C16,12.418 12.418,16 8,16 3.582,16 0,12.418 0,8 0,3.582 3.582,0 8,0 12.418,0 16,3.582 16,8" />
                    <GeometryDrawing Brush="#FFE41400" Geometry="F1M12.4141,11L11.0001,12.414 8.0001,9.414 5.0001,12.414 3.5861,11 6.5861,8 3.5861,5 5.0001,3.586 8.0001,6.586 11.0001,3.586 12.4141,5 9.4141,8z M8.0001,1C4.1341,1 1.0001,4.135 1.0001,8 1.0001,11.865 4.1341,15 8.0001,15 11.8651,15 15.0001,11.865 15.0001,8 15.0001,4.135 11.8651,1 8.0001,1" />
                    <GeometryDrawing Brush="#FFFFFFFF" Geometry="F1M9.4141,8L12.4141,11 11.0001,12.414 8.0001,9.414 5.0001,12.414 3.5861,11 6.5861,8 3.5861,5 5.0001,3.586 8.0001,6.586 11.0001,3.586 12.4141,5z" />
                </DrawingGroup.Children>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <DrawingImage x:Key="TSIconInfo">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <DrawingGroup.Children>
                    <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
                    <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M0,8.0005C0,3.5825 3.582,0.000499999999998835 8,0.000499999999998835 12.418,0.000499999999998835 16,3.5825 16,8.0005 16,12.4185 12.418,16.0005 8,16.0005 3.582,16.0005 0,12.4185 0,8.0005" />
                    <GeometryDrawing Brush="#FF1AA1E2" Geometry="F1M9,5L7,5 7,3 9,3z M9,13L7,13 7,6 9,6z M8,1C4.135,1 1,4.135 1,8 1,11.865 4.135,15 8,15 11.865,15 15,11.865 15,8 15,4.135 11.865,1 8,1" />
                    <GeometryDrawing Brush="#FFFFFFFF" Geometry="F1M7,5L9,5 9,3 7,3z M7,6L9,6 9,13 7,13z" />
                </DrawingGroup.Children>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <DrawingImage x:Key="TSIconRestoreWindow">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <DrawingGroup.Children>
                    <GeometryDrawing Brush="{Binding ButtonText, Source={x:Static services:UIStylesService.Instance}}" Geometry="M 10 7.9980469 H 7.9980469 V 10 H 0 V 2.0019531 H 2.0019531 V 0 H 10 Z m -2.9980469 -5 H 1.0009766 V 8.9990234 H 7.0019531 Z M 8.9990234 1.0009766 H 2.9980469 v 1.0009765 h 5 v 5 h 1.0009765 z" />
                </DrawingGroup.Children>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <DrawingImage x:Key="TSIconMaximizeWindow">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <DrawingGroup.Children>
                    <GeometryDrawing Brush="{Binding ButtonText, Source={x:Static services:UIStylesService.Instance}}" Geometry="M 10 0 V 10 H 0 V 0 Z M 8.9990234 1.0009766 H 1.0009766 v 7.9980468 h 7.9980468 z" />
                </DrawingGroup.Children>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <DrawingImage x:Key="TSIconCloseWindow">
        <DrawingImage.Drawing>
            <DrawingGroup>
                <DrawingGroup.Children>
                    <GeometryDrawing Brush="{Binding ButtonText, Source={x:Static services:UIStylesService.Instance}}" Geometry="M 5.7080078 5 10 9.2919922 9.2919922 10 5 5.7080078 0.70800781 10 0 9.2919922 4.2919922 5 0 0.70800781 0.70800781 0 5 4.2919922 9.2919922 0 10 0.70800781 Z" />
                </DrawingGroup.Children>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>
    <!--#endregion-->

    <!--#region Thickness-->

    <Thickness x:Key="Margin_XS_LeftTopRightBottom">4,4,4,4</Thickness>
    <Thickness x:Key="Margin_XS_Left">4,0,0,0</Thickness>
    <Thickness x:Key="Margin_XS_Right">0,0,4,0</Thickness>
    <Thickness x:Key="Margin_XS_LeftRight">4,0,4,0</Thickness>
    <Thickness x:Key="Margin_XS_Bottom">0,0,0,4</Thickness>
    <Thickness x:Key="Margin_XS_Top">0,4,0,0</Thickness>
    <Thickness x:Key="Margin_XS_TopBottom">0,4,0,4</Thickness>

    <Thickness x:Key="Margin_S_Left">8,0,0,0</Thickness>
    <Thickness x:Key="Margin_S_Right">0,0,8,0</Thickness>
    <Thickness x:Key="Margin_S_LeftTopRightBottom">8,8,8,8</Thickness>
    <Thickness x:Key="Margin_S_LeftTop">8,8,0,0</Thickness>
    <Thickness x:Key="Margin_S_Top">0,8,0,0</Thickness>
    <Thickness x:Key="Margin_S_TopBottom">0,8,0,8</Thickness>
    <Thickness x:Key="Margin_S_Bottom">0,0,0,8</Thickness>

    <Thickness x:Key="Margin_M_Left">12,0,0,0</Thickness>
    <Thickness x:Key="Margin_M_LeftTopRightBottom">12,12,12,12</Thickness>
    <Thickness x:Key="Margin_M_LeftTopRight">12,12,12,0</Thickness>
    <Thickness x:Key="Margin_M_LeftTop">12,12,0,0</Thickness>
    <Thickness x:Key="Margin_M_LeftRight">12,0,12,0</Thickness>
    <Thickness x:Key="Margin_M_Right">0,0,12,0</Thickness>
    <Thickness x:Key="Margin_M_Top">0,12,0,0</Thickness>

    <Thickness x:Key="Margin_M_Left_S_TopBottom">12,8,0,8</Thickness>



    <Thickness x:Key="Margin_M1_LeftRight">14,0,14,0</Thickness>
    <Thickness x:Key="Margin_M1_LeftRight_S_Top">14,8,14,0</Thickness>

    <Thickness x:Key="Margin_M2_LeftRight_S_Top">16,8,16,0</Thickness>
    <Thickness x:Key="Margin_M2_LeftRight_S_TopBottom">16,8,16,8</Thickness>
    <Thickness x:Key="Margin_M2_LeftTopRightBottom">16,16,16,16</Thickness>
    <Thickness x:Key="Margin_M2_LeftRight">16,0,16,0</Thickness>
    <Thickness x:Key="Margin_M2_LeftRight_M_Top">16,12,16,0</Thickness>
    <Thickness x:Key="Margin_M2_LeftRight_M_TopBottom">16,12,16,12</Thickness>


    <Thickness x:Key="Margin_M2_LeftTopRight">16,16,16,0</Thickness>
    <Thickness x:Key="Margin_M2_LeftRight_L_Top">16,24,16,0</Thickness>

    <Thickness x:Key="Margin_M3_LeftTop">20,20,0,0</Thickness>
    <Thickness x:Key="Margin_M3_LeftTopRightBottom">20,20,20,20</Thickness>

    <Thickness x:Key="Margin_L_Left">24,0,0,0</Thickness>
    <Thickness x:Key="Margin_L_LeftTop">24,24,0,0</Thickness>
    <Thickness x:Key="Margin_L_LeftRight">24,0,24,0</Thickness>
    <Thickness x:Key="Margin_L_Top">0,24,0,0</Thickness>

    <Thickness x:Key="Margin_L_LeftRight_M_Top">24,12,24,0</Thickness>
    <Thickness x:Key="Margin_L_LeftRight_M2_Top">24,16,24,0</Thickness>

    <Thickness x:Key="CardBorderThickness">1.5,1.5,1.5,1.5</Thickness>

    <!--#endregion-->

    <!--#region Image-->

    <Style x:Key="TSImageNewItemFileIcon" TargetType="Image">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Stretch" Value="None" />
        <Setter Property="RenderOptions.BitmapScalingMode" Value="NearestNeighbor" />
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.AccessibilityNewItemFileIcon}" />
    </Style>

    <Style x:Key="TSImageNewItemImageFiles" TargetType="Image">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Stretch" Value="None" />
        <Setter Property="RenderOptions.BitmapScalingMode" Value="NearestNeighbor" />
    </Style>

    <Style x:Key="TSImageDialogIcon" TargetType="Image">
        <Setter Property="Width" Value="40" />
        <Setter Property="Height" Value="40" />
        <Setter Property="StretchDirection" Value="Both" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="Margin" Value="{StaticResource Margin_M_Right}" />
    </Style>

    <Style x:Key="TSImageNotificationIcon" TargetType="Image">
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
        <Setter Property="StretchDirection" Value="Both" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="{StaticResource Margin_M_Left}" />
    </Style>

    <Style x:Key="TSImageRestoreButtonIcon" TargetType="Image">
        <Setter Property="Width" Value="10" />
        <Setter Property="Height" Value="10" />
        <Setter Property="Stretch" Value="None" />
    </Style>

    <Style x:Key="TSImageMaximizeButtonIcon" TargetType="Image">
        <Setter Property="Width" Value="10" />
        <Setter Property="Height" Value="10" />
        <Setter Property="Stretch" Value="None" />
    </Style>

    <Style x:Key="TSImageCloseButtonIcon" TargetType="Image">
        <Setter Property="Width" Value="10" />
        <Setter Property="Height" Value="10" />
        <Setter Property="Stretch" Value="None" />
    </Style>


    <Style x:Key="TSBreakingChangesImageIcon" TargetType="Image">
        <Setter Property="Width" Value="20" />
        <Setter Property="Height" Value="20" />
        <Setter Property="StretchDirection" Value="Both" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="TSCrispImageCardDeprecatedIcon" TargetType="imaging:CrispImage">
        <Setter Property="Height" Value="16" />
        <Setter Property="Width" Value="16" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="{StaticResource Margin_XS_Right}" />
    </Style>

    <Style x:Key="TSCrispImageTemplateDetailsDeprecatedIcon" TargetType="imaging:CrispImage">
        <Setter Property="Height" Value="20" />
        <Setter Property="Width" Value="20" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="{StaticResource Margin_S_Right}" />
    </Style>

    <!--#endregion-->

    <!--#region Button-->

    <Style x:Key="TSBaseButton" TargetType="Button">
        <Setter Property="Foreground" Value="{Binding ButtonText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Background" Value="{Binding Button, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderBrush" Value="{Binding ButtonBorder, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="1" />
        <Setter Property="FocusVisualStyle">
            <Setter.Value>
                <Style>
                    <Setter Property="Control.Template">
                        <Setter.Value>
                            <ControlTemplate />
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDefaulted" Value="true">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding ButtonHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="{Binding ButtonHoverText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding ButtonBorderHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding ButtonPressed, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="{Binding ButtonPressedText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding ButtonBorderPressed, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="border" Property="Background" Value="{Binding ButtonDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="{Binding ButtonDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding ButtonBorderDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property="Background" Value="{Binding ButtonFocused, Source={x:Static services:UIStylesService.Instance}}" />
                <Setter Property="TextElement.Foreground" Value="{Binding ButtonFocusedText, Source={x:Static services:UIStylesService.Instance}}" />
                <Setter Property="BorderBrush" Value="{Binding ButtonBorderFocused, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="TSButton"
        BasedOn="{StaticResource TSBaseButton}"
        TargetType="Button">
        <Setter Property="Width" Value="75" />
        <Setter Property="Height" Value="23" />
    </Style>

    <Style
        x:Key="TSButtonFinish"
        BasedOn="{StaticResource TSButton}"
        TargetType="Button">
        <Setter Property="BorderBrush" Value="{Binding ButtonPressed, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSButtonIcon" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="1" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="FocusVisualStyle">
            <Setter.Value>
                <Style>
                    <Setter Property="Control.Template">
                        <Setter.Value>
                            <ControlTemplate />
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDefaulted" Value="true" />
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding ButtonHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{Binding ButtonFocused, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter Property="BorderBrush" Value="{Binding ButtonBorderFocused, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding ButtonPressed, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{Binding ButtonDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true" />
        </Style.Triggers>
    </Style>

    <Style x:Key="TSButtonCloseNotification" TargetType="Button">
        <Setter Property="Background" Value="{Binding IBCloseButton, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderBrush" Value="{Binding IBCloseButtonBorder, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding IBCloseButtonGlyph, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="Margin" Value="{StaticResource Margin_M_Right}" />
        <Setter Property="Padding" Value="{StaticResource Margin_S_LeftTopRightBottom}" />
        <Setter Property="Content" Value="&#xE106;" />
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.ButtonClose}" />
        <Setter Property="ToolTip" Value="{x:Static res:Resources.ButtonClose}" />

        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDefaulted" Value="true" />
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding IBCloseButtonHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding IBCloseButtonHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter Property="Foreground" Value="{Binding IBCloseButtonHoverGlyph, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding IBCloseButtonDown, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding IBCloseButtonDownBorder, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter Property="Foreground" Value="{Binding IBCloseButtonDownGlyph, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="border" Property="Background" Value="{Binding IBButtonDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding IBButtonDisabledBorder, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding IBButtonFocus, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding IBButtonFocusBorder, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="TSButtonDeleteTemplate"
        BasedOn="{StaticResource TSButtonIcon}"
        TargetType="Button">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="{StaticResource Margin_XS_LeftTopRightBottom}" />
        <Setter Property="Foreground" Value="{Binding DeleteTemplateIcon, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Content" Value="&#xE10A;" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.ButtonRemove}" />
        <Setter Property="ToolTip" Value="{x:Static res:Resources.ButtonRemove}" />
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property="Foreground" Value="{Binding ButtonFocusedText, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="TSButtonWindowButtons"
        BasedOn="{StaticResource TSBaseButton}"
        TargetType="Button">
        <Setter Property="Padding" Value="16,12,16,12" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style
        x:Key="TSButtonRestoreWindow"
        BasedOn="{StaticResource TSButtonWindowButtons}"
        TargetType="Button">
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.ButtonRestore}" />
        <Setter Property="ToolTip" Value="{x:Static res:Resources.ButtonRestore}" />
        <Setter Property="Content">
            <Setter.Value>
                <Image Source="{StaticResource TSIconRestoreWindow}" Style="{StaticResource TSImageRestoreButtonIcon}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="TSButtonMaximizeWindow"
        BasedOn="{StaticResource TSButtonWindowButtons}"
        TargetType="Button">
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.ButtonMaximize}" />
        <Setter Property="ToolTip" Value="{x:Static res:Resources.ButtonMaximize}" />
        <Setter Property="Content">
            <Setter.Value>
                <Image Source="{StaticResource TSIconMaximizeWindow}" Style="{StaticResource TSImageMaximizeButtonIcon}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="TSButtonCloseWindow"
        BasedOn="{StaticResource TSButtonWindowButtons}"
        TargetType="Button">
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.ButtonClose}" />
        <Setter Property="ToolTip" Value="{x:Static res:Resources.ButtonClose}" />
        <Setter Property="Content">
            <Setter.Value>
                <Image Source="{StaticResource TSIconCloseWindow}" Style="{StaticResource TSImageCloseButtonIcon}" />
            </Setter.Value>
        </Setter>
    </Style>


    <Style
        x:Key="ButtonRefreshTemplatesStyle"
        BasedOn="{StaticResource TSBaseButton}"
        TargetType="Button">
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Height" Value="23" />
    </Style>
    <!--#endregion-->

    <!--#region CheckBox-->

    <Style x:Key="TSCheckBoxStyle" TargetType="{x:Type CheckBox}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Background" Value="{Binding CheckBoxBackground, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderBrush" Value="{Binding CheckBoxBorder, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding CheckBoxText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid
                        x:Name="templateRoot"
                        Background="Transparent"
                        SnapsToDevicePixels="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border
                            x:Name="checkBoxBorder"
                            Margin="1"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid x:Name="markGrid">
                                <Path
                                    x:Name="optionMark"
                                    Margin="1"
                                    Data="F1 M 9.97498,1.22334L 4.6983,9.09834L 4.52164,9.09834L 0,5.19331L 1.27664,3.52165L 4.255,6.08833L 8.33331,1.52588e-005L 9.97498,1.22334 Z "
                                    Fill="{Binding CheckBoxGlyph, Source={x:Static services:UIStylesService.Instance}}"
                                    Opacity="0"
                                    Stretch="None" />
                                <Rectangle
                                    x:Name="indeterminateMark"
                                    Margin="2"
                                    Fill="{Binding CheckBoxGlyph, Source={x:Static services:UIStylesService.Instance}}"
                                    Opacity="0" />
                            </Grid>
                        </Border>
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Grid.Column="1"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasContent" Value="true">
                            <Setter Property="Padding" Value="4,-1,0,0" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="checkBoxBorder" Property="Background" Value="{Binding CheckBoxBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{Binding CheckBoxBorderHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="optionMark" Property="Fill" Value="{Binding CheckBoxGlyphHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="indeterminateMark" Property="Fill" Value="{Binding CheckBoxGlyphHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="checkBoxBorder" Property="Background" Value="{Binding CheckBoxBackgroundDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{Binding CheckBoxBorderDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="optionMark" Property="Fill" Value="{Binding CheckBoxGlyphDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="indeterminateMark" Property="Fill" Value="{Binding CheckBoxGlyphDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="checkBoxBorder" Property="Background" Value="{Binding CheckBoxBackgroundPressed, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{Binding CheckBoxBorderPressed, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="optionMark" Property="Fill" Value="{Binding CheckBoxGlyphPressed, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="indeterminateMark" Property="Fill" Value="{Binding CheckBoxGlyphPressed, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="checkBoxBorder" Property="Background" Value="{Binding CheckBoxBackgroundFocused, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{Binding CheckBoxBorderFocused, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="optionMark" Property="Fill" Value="{Binding CheckBoxGlyphFocused, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="indeterminateMark" Property="Fill" Value="{Binding CheckBoxGlyphFocused, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="optionMark" Property="Opacity" Value="1" />
                            <Setter TargetName="indeterminateMark" Property="Opacity" Value="0" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="optionMark" Property="Opacity" Value="0" />
                            <Setter TargetName="indeterminateMark" Property="Opacity" Value="1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--#endregion-->

    <!--#region ImageEx-->

    <Style x:Key="TSImageEx" TargetType="controls:ImageEx">
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{Binding CardIcon, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FallbackImage" Value="/SharedResources;component/Assets/DefaultIcon.png" />
        <Setter Property="MaxHeight" Value="48" />
        <Setter Property="MaxWidth" Value="70" />
    </Style>

    <Style
        x:Key="TSImageExWithDisabled"
        BasedOn="{StaticResource TSImageEx}"
        TargetType="controls:ImageEx">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Disabled}" Value="true">
                <Setter Property="Foreground" Value="{Binding ListItemDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="TSImageExProjectDetails"
        BasedOn="{StaticResource TSImageEx}"
        TargetType="controls:ImageEx">
        <Setter Property="MaxHeight" Value="20" />
        <Setter Property="MaxWidth" Value="20" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!--#endregion-->

    <!--#region TextBlock-->

    <!--  Global  -->
    <Style x:Key="TSTextBlockWizardTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment200PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding HeaderText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style
        x:Key="TSTextBlockWizardTitleSecondary"
        BasedOn="{StaticResource TSTextBlockWizardTitle}"
        TargetType="TextBlock">
        <Setter Property="Foreground" Value="{Binding HeaderTextSecondary, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSTextBlockSectionTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment122PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style x:Key="TSTextBlockSubSectionTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style x:Key="TSTextBlockSubSectionText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style
        x:Key="TSTextBlockAboutSubSectionText"
        BasedOn="{StaticResource TSTextBlockSubSectionText}"
        TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSTextBlockTermsAndConditions" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WizardFooterText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Margin" Value="26,0,0,0" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>


    <!--  Sequential Flow  -->
    <Style x:Key="TSTextBlockSequentialFlow" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="10,5,0,5" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style x:Key="TSTextBlockSequentialFlowCheckMark" TargetType="TextBlock">
        <Setter Property="Text" Value="&#xE001;" />
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="10,5,10,5" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <!--  Card  -->
    <Style x:Key="TSTextBlockCardTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment122PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding CardTitleText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style
        x:Key="TSTextBlockCardTitleWithDisabled"
        BasedOn="{StaticResource TSTextBlockCardTitle}"
        TargetType="TextBlock">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Disabled}" Value="true">
                <Setter Property="Foreground" Value="{Binding ListItemDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
            </DataTrigger>
        </Style.Triggers>

    </Style>

    <Style x:Key="TSTextBlockCardDescription" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding CardDescriptionText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style
        x:Key="TSTextBlockCardDescriptionWithDisabled"
        BasedOn="{StaticResource TSTextBlockCardDescription}"
        TargetType="TextBlock">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Disabled}" Value="true">
                <Setter Property="Foreground" Value="{Binding ListItemDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TSTextBlockCardAddedText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding CardFooterText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style x:Key="TSTextBlockCardMouseOverAddIcon" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{Binding CardBorderSelected, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Text" Value="&#xE109;" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <Style x:Key="TSTextBlockCardTemplatesCount" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{Binding CardBackgroundDefault, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <Style x:Key="TSTextBlockCardAddedIcon" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{Binding CardBackgroundDefault, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Text" Value="&#xE001;" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
    </Style>

    <Style x:Key="TSRunHyperlink" TargetType="Run">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding Hyperlink, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="BaselineAlignment" Value="Center" />
    </Style>
    <Style x:Key="TSTextBlockHyperlink" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding Hyperlink, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <Style
        x:Key="TSTextBlockHyperlinkAbout"
        BasedOn="{StaticResource TSTextBlockHyperlink}"
        TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSTextBlockCardDeprecatedMessage" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding CardTitleText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style
        x:Key="TSTextBlockCardDeprecatedMessageWithDisabled"
        BasedOn="{StaticResource TSTextBlockCardDeprecatedMessage}"
        TargetType="TextBlock">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Disabled}" Value="true">
                <Setter Property="Foreground" Value="{Binding ListItemDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--  Project Details  -->
    <Style x:Key="TSTextBlockComboBoxItem" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style x:Key="TSTextBlockSavedTemplateName" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding ListItemText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style x:Key="TSTextBlockDragAndDropTemplate" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{Binding CardIcon, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Text" Value="&#xE784;" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <!--  TemplateDetails  -->
    <Style x:Key="TSTextBlockTemplateDetailsTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment133PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSTextBlockTemplateDetailsDisabledText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment122PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding TemplateInfoPageDescription, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSTextBlockTemplateDetailsPropertyLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="Light" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSTextBlockTemplateDetailsPropertyValue" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSTextBlockNewItemFileSubject" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TSTextBlockTemplateDetailsDeprecatedMessage" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontSize" Value="{Binding Environment122PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Focusable" Value="False" />
    </Style>

    <!--  Changes Summary  -->
    <Style x:Key="TSTextBlockFileStatusDescription" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
    </Style>

    <Style x:Key="TSTextBlockChangesSummaryDetailFileHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding ChangesSummaryDetailFileHeaderText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!--  Dialog  -->
    <Style x:Key="TSTextBlockDialogTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment155PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding HeaderText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextAlignment" Value="Left" />
    </Style>

    <Style x:Key="TSTextBlockDialogDescription" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="TextTrimming" Value="WordEllipsis" />
        <Setter Property="TextAlignment" Value="Left" />
    </Style>

    <!--  Project Configuration  -->
    <Style x:Key="TSTextBlockProjectConfigurationDescriptionStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding HeaderText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextAlignment" Value="Justify" />
        <Setter Property="TextWrapping" Value="Wrap" />

    </Style>

    <Style x:Key="TSTextBlockProjectConfigurationComboBoxHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <!--  Composition Tool  -->
    <Style x:Key="TSTextBlockCompositionToolTreeViewText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Margin" Value="0,4" />
    </Style>

    <!--  Bullet  -->
    <Style x:Key="TSTextBlockBullet" TargetType="TextBlock">
        <Setter Property="Text" Value="&#xE915;" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
    </Style>

    <!--#endregion-->

    <!--#region ComboBox-->

    <Style x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="ClickMode" Value="Press" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border
                        x:Name="templateRoot"
                        Background="{Binding ComboBoxBackground, Source={x:Static services:UIStylesService.Instance}}"
                        BorderBrush="{Binding ComboBoxBorder, Source={x:Static services:UIStylesService.Instance}}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <Border
                            x:Name="splitBorder"
                            Width="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"
                            Margin="0"
                            HorizontalAlignment="Right"
                            BorderBrush="Transparent"
                            BorderThickness="1"
                            SnapsToDevicePixels="true">
                            <Path
                                x:Name="arrow"
                                Margin="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="F1 M 0,0 L 2.667,2.66665 L 5.3334,0 L 5.3334,-1.78168 L 2.6667,0.88501 L0,-1.78168 L0,0 Z"
                                Fill="{Binding ComboBoxGlyph, Source={x:Static services:UIStylesService.Instance}}" />
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="arrow" Property="Fill" Value="{Binding ComboBoxGlyphHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="Background" Value="{Binding ComboBoxBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{Binding ComboBoxSelection, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="arrow" Property="Fill" Value="{Binding ComboBoxGlyphPressed, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="Background" Value="{Binding ComboBoxBackgroundPressed, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{Binding ComboBoxBorderPressed, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="arrow" Property="Fill" Value="{Binding ComboBoxGlyphDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="Background" Value="{Binding ComboBoxBackgroundDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{Binding ComboBoxBorderDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsKeyboardFocused, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ComboBox}}}" Value="True">
                            <Setter TargetName="arrow" Property="Fill" Value="{Binding ComboBoxGlyphHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="Background" Value="{Binding ComboBoxBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{Binding ComboBoxSelection, Source={x:Static services:UIStylesService.Instance}}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AutomationProperties.Name" Value="{Binding Title}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border
                        x:Name="Bd"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter TargetName="Bd" Property="TextElement.Foreground" Value="{Binding ComboBoxListItemText, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="TextElement.Foreground" Value="{Binding ComboBoxListItemTextHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="Background" Value="{Binding ComboBoxListItemBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{Binding ComboBoxListItemBorderHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="TextElement.Foreground" Value="{Binding ComboBoxListItemTextHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="Background" Value="{Binding ComboBoxListItemBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{Binding ComboBoxListItemBorderHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="Bd" Property="TextElement.Foreground" Value="{Binding ComboBoxBackground, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="Background" Value="{Binding ComboBoxSelection, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{Binding ComboBoxSelection, Source={x:Static services:UIStylesService.Instance}}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSComboBoxStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Background" Value="{Binding ComboBoxBackground, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderBrush" Value="{Binding ComboBoxBorder, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding ComboBoxText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="Padding" Value="6,3,5,3" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSComboBoxItemStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="0" MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
                        </Grid.ColumnDefinitions>
                        <Popup
                            x:Name="PART_Popup"
                            Grid.ColumnSpan="2"
                            Margin="1"
                            AllowsTransparency="true"
                            IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            Placement="Bottom"
                            PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                            <Themes:SystemDropShadowChrome
                                x:Name="shadow"
                                MinWidth="{Binding ActualWidth, ElementName=templateRoot}"
                                MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                Color="Transparent">
                                <Border
                                    x:Name="dropDownBorder"
                                    Background="{Binding ComboBoxBackground, Source={x:Static services:UIStylesService.Instance}}"
                                    BorderBrush="{Binding ComboBoxBorder, Source={x:Static services:UIStylesService.Instance}}"
                                    BorderThickness="1">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                            <Canvas
                                                x:Name="canvas"
                                                Width="0"
                                                Height="0"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Top">
                                                <Rectangle
                                                    x:Name="opaqueRect"
                                                    Width="{Binding ActualWidth, ElementName=dropDownBorder}"
                                                    Height="{Binding ActualHeight, ElementName=dropDownBorder}"
                                                    Fill="{Binding Background, ElementName=dropDownBorder}" />
                                            </Canvas>
                                            <ItemsPresenter
                                                x:Name="ItemsPresenter"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                            <!--  KeyboardNavigation.DirectionalNavigation="Contained"  -->
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Themes:SystemDropShadowChrome>
                        </Popup>
                        <ToggleButton
                            x:Name="toggleButton"
                            Grid.ColumnSpan="2"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                            Style="{StaticResource ComboBoxToggleButton}" />
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding SelectionBoxItem}"
                            ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                            IsHitTestVisible="false"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger SourceName="PART_Popup" Property="HasDropShadow" Value="true">
                            <Setter TargetName="shadow" Property="Margin" Value="0,0,5,5" />
                            <Setter TargetName="shadow" Property="Color" Value="{Binding ComboBoxListBackgroundShadow, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="HasItems" Value="false">
                            <Setter TargetName="dropDownBorder" Property="Height" Value="95" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </MultiTrigger>
                        <Trigger SourceName="DropDownScrollViewer" Property="ScrollViewer.CanContentScroll" Value="false">
                            <Setter TargetName="opaqueRect" Property="Canvas.Top" Value="{Binding VerticalOffset, ElementName=DropDownScrollViewer}" />
                            <Setter TargetName="opaqueRect" Property="Canvas.Left" Value="{Binding HorizontalOffset, ElementName=DropDownScrollViewer}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property="Foreground" Value="{Binding ComboBoxTextHover, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>
        </Style.Triggers>
    </Style>


    <!--  ProjectType and Framework  -->
    <Style x:Key="ComboBoxMetadataToggleButton" TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="ClickMode" Value="Press" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border
                        x:Name="templateRoot"
                        Background="Transparent"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSComboBoxItemMetadataStyle" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AutomationProperties.Name" Value="{Binding Title}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border
                        x:Name="Bd"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        SnapsToDevicePixels="true">
                        <!--<ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />-->
                        <Grid
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <controls:ImageEx
                                x:Name="icon"
                                Grid.Column="0"
                                SourcePath="{Binding Icon}"
                                Style="{StaticResource TSImageExProjectDetails}" />
                            <TextBlock
                                x:Name="name"
                                Grid.Column="1"
                                Margin="{StaticResource Margin_M_Left}"
                                Style="{StaticResource TSTextBlockComboBoxItem}"
                                Text="{Binding Title}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter TargetName="name" Property="Foreground" Value="{Binding ComboBoxListItemText, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="name" Property="Foreground" Value="{Binding ListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="Background" Value="{Binding ListItemMouseOver, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="name" Property="Foreground" Value="{Binding ListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="Background" Value="{Binding ListItemMouseOver, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsKeyboardFocused, RelativeSource={RelativeSource Self}}" Value="true" />
                                <Condition Binding="{Binding IsHighContrast, Source={x:Static services:SystemService.Current}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="icon" Property="Foreground" Value="{Binding ListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true" />
                                <Condition Binding="{Binding IsHighContrast, Source={x:Static services:SystemService.Current}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="icon" Property="Foreground" Value="{Binding ListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter TargetName="icon" Property="Foreground" Value="{Binding SelectedItemActiveText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="name" Property="Foreground" Value="{Binding SelectedItemActiveText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="Bd" Property="Background" Value="{Binding SelectedItemActive, Source={x:Static services:UIStylesService.Instance}}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSComboBoxProjectTypeStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Background" Value="{Binding ComboBoxBackground, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderBrush" Value="{Binding ComboBoxBorder, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding ComboBoxText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="Padding" Value="6,3,5,3" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSComboBoxItemMetadataStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Border
                        x:Name="templateRoot"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="0" MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
                            </Grid.ColumnDefinitions>
                            <Grid
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                IsHitTestVisible="false"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <controls:ImageEx
                                    x:Name="icon"
                                    Grid.Column="0"
                                    SourcePath="{Binding ProjectType.Selected.Icon}"
                                    Style="{StaticResource TSImageExProjectDetails}" />
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="{StaticResource Margin_M_Left}"
                                    Foreground="{TemplateBinding Foreground}"
                                    Style="{StaticResource TSTextBlockComboBoxItem}"
                                    Text="{Binding ProjectType.Selected.Title}" />
                            </Grid>
                            <Button
                                x:Name="removeButton"
                                Grid.Column="1"
                                Height="Auto"
                                Margin="0,0,5,0"
                                HorizontalAlignment="Right"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                SnapsToDevicePixels="true">
                                <Button.Style>
                                    <Style TargetType="{x:Type Button}">
                                        <Setter Property="Background" Value="Transparent" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type Button}">
                                                    <Border Background="{TemplateBinding Background}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="Transparent" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Path
                                    x:Name="remove"
                                    Width="10"
                                    Height="10"
                                    Margin="0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="M 11.416016,10 20,1.4160156 18.583984,0 10,8.5839846 1.4160156,0 0,1.4160156 8.5839844,10 0,18.583985 1.4160156,20 10,11.416015 18.583984,20 20,18.583985 Z"
                                    Fill="{Binding ComboBoxGlyph, Source={x:Static services:UIStylesService.Instance}}"
                                    Stretch="Uniform" />
                            </Button>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="icon" Property="Foreground" Value="{Binding ComboBoxTextHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter Property="TextElement.Foreground" Value="{Binding ComboBoxTextHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="Background" Value="{Binding ComboBoxBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{Binding ComboBoxSelection, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="remove" Property="Fill" Value="{Binding ComboBoxGlyphHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSComboBoxFrameworkStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Background" Value="{Binding ComboBoxBackground, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderBrush" Value="{Binding ComboBoxBorder, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding ComboBoxText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="Padding" Value="6,3,5,3" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="Both" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSComboBoxItemMetadataStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Border
                        x:Name="templateRoot"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="0" MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
                            </Grid.ColumnDefinitions>
                            <Popup
                                x:Name="PART_Popup"
                                Grid.ColumnSpan="2"
                                Margin="1"
                                AllowsTransparency="true"
                                IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                Placement="Bottom"
                                PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                                <Themes:SystemDropShadowChrome
                                    x:Name="shadow"
                                    MinWidth="{Binding ActualWidth, ElementName=templateRoot}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    Color="Transparent">
                                    <Border
                                        x:Name="dropDownBorder"
                                        Background="{Binding ComboBoxBackground, Source={x:Static services:UIStylesService.Instance}}"
                                        BorderBrush="{Binding ComboBoxBorder, Source={x:Static services:UIStylesService.Instance}}"
                                        BorderThickness="1">
                                        <ScrollViewer x:Name="DropDownScrollViewer">
                                            <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                                <Canvas
                                                    x:Name="canvas"
                                                    Width="0"
                                                    Height="0"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Top">
                                                    <Rectangle
                                                        x:Name="opaqueRect"
                                                        Width="{Binding ActualWidth, ElementName=dropDownBorder}"
                                                        Height="{Binding ActualHeight, ElementName=dropDownBorder}"
                                                        Fill="{Binding Background, ElementName=dropDownBorder}" />
                                                </Canvas>
                                                <ItemsPresenter
                                                    x:Name="ItemsPresenter"
                                                    KeyboardNavigation.DirectionalNavigation="Cycle"
                                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                <!--  KeyboardNavigation.DirectionalNavigation="Contained"  -->
                                            </Grid>
                                        </ScrollViewer>
                                    </Border>
                                </Themes:SystemDropShadowChrome>
                            </Popup>
                            <ToggleButton
                                x:Name="toggleButton"
                                Grid.ColumnSpan="2"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                Style="{StaticResource ComboBoxMetadataToggleButton}" />
                            <Grid
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                IsHitTestVisible="false"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <controls:ImageEx
                                    x:Name="icon"
                                    Grid.Column="0"
                                    SourcePath="{Binding Framework.Selected.Icon}"
                                    Style="{StaticResource TSImageExProjectDetails}" />
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="{StaticResource Margin_M_Left}"
                                    Foreground="{TemplateBinding Foreground}"
                                    Style="{StaticResource TSTextBlockComboBoxItem}"
                                    Text="{Binding Framework.Selected.Title}" />
                            </Grid>
                            <Border
                                Grid.Column="1"
                                Width="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"
                                Margin="0"
                                HorizontalAlignment="Right"
                                BorderBrush="Transparent"
                                SnapsToDevicePixels="true">
                                <Path
                                    x:Name="arrow"
                                    Margin="0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="F1 M 0,0 L 2.667,2.66665 L 5.3334,0 L 5.3334,-1.78168 L 2.6667,0.88501 L0,-1.78168 L0,0 Z"
                                    Fill="{Binding ComboBoxGlyph, Source={x:Static services:UIStylesService.Instance}}" />
                            </Border>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="icon" Property="Foreground" Value="{Binding ComboBoxTextHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter Property="TextElement.Foreground" Value="{Binding ComboBoxTextHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="Background" Value="{Binding ComboBoxBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{Binding ComboBoxSelection, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="arrow" Property="Fill" Value="{Binding ComboBoxGlyphHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="icon" Property="Foreground" Value="{Binding ComboBoxTextHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter Property="TextElement.Foreground" Value="{Binding ComboBoxTextHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="Background" Value="{Binding ComboBoxBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{Binding ComboBoxSelection, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="arrow" Property="Fill" Value="{Binding ComboBoxGlyphHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger SourceName="PART_Popup" Property="HasDropShadow" Value="true">
                            <Setter TargetName="shadow" Property="Margin" Value="0,0,5,5" />
                            <Setter TargetName="shadow" Property="Color" Value="{Binding ComboBoxListBackgroundShadow, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="HasItems" Value="false">
                            <Setter TargetName="dropDownBorder" Property="Height" Value="95" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </MultiTrigger>
                        <Trigger SourceName="DropDownScrollViewer" Property="ScrollViewer.CanContentScroll" Value="false">
                            <Setter TargetName="opaqueRect" Property="Canvas.Top" Value="{Binding VerticalOffset, ElementName=DropDownScrollViewer}" />
                            <Setter TargetName="opaqueRect" Property="Canvas.Left" Value="{Binding HorizontalOffset, ElementName=DropDownScrollViewer}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--#endregion-->

    <!--#region Expander-->

    <Style x:Key="ExpanderDownHeaderStyle" TargetType="{x:Type ToggleButton}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border
                        x:Name="border"
                        Padding="{StaticResource Margin_S_LeftTopRightBottom}"
                        Background="{TemplateBinding Background}">
                        <Grid SnapsToDevicePixels="False">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Path
                                x:Name="arrow"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="M 0,0 L 5,5 L 10,0"
                                SnapsToDevicePixels="false"
                                Stroke="{Binding PageSideBarExpanderChevron, Source={x:Static services:UIStylesService.Instance}}"
                                StrokeThickness="2" />
                            <ContentPresenter
                                Grid.Column="1"
                                Margin="{StaticResource Margin_S_Left}"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="True" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="arrow" Property="Data" Value="M 0,0  L 5,5  L 0,10" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding PageSideBarExpanderHeaderHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding PageSideBarExpanderHeaderHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding PageSideBarExpanderHeaderPressed, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false" />
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSExpander" TargetType="{x:Type Expander}">
        <Setter Property="Foreground" Value="{Binding PageSideBarExpanderText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Background" Value="{Binding PageSideBarExpanderBody, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsExpanded" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Expander}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="3"
                        SnapsToDevicePixels="true">
                        <DockPanel>
                            <ToggleButton
                                x:Name="HeaderSite"
                                MinWidth="0"
                                MinHeight="0"
                                Margin="1"
                                Padding="{TemplateBinding Padding}"
                                HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                Background="{Binding PageSideBarExpanderHeader, Source={x:Static services:UIStylesService.Instance}}"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                DockPanel.Dock="Top"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="{TemplateBinding FontSize}"
                                FontStretch="{TemplateBinding FontStretch}"
                                FontStyle="{TemplateBinding FontStyle}"
                                FontWeight="{TemplateBinding FontWeight}"
                                Foreground="{TemplateBinding Foreground}"
                                IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                Style="{StaticResource ExpanderDownHeaderStyle}" />
                            <ContentPresenter
                                x:Name="ExpandSite"
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                DockPanel.Dock="Bottom"
                                Focusable="false"
                                Visibility="Collapsed" />
                        </DockPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true" />
                        <Trigger Property="IsExpanded" Value="true">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--#endregion-->

    <!--#region Grid-->

    <Style x:Key="TSGridBasePage" TargetType="Grid">
        <Setter Property="Background" Value="{Binding WindowPanel, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style
        x:Key="TSGridMainPage"
        BasedOn="{StaticResource TSGridBasePage}"
        TargetType="Grid" />

    <Style x:Key="TSGridMainFooter" TargetType="Grid">
        <Setter Property="Background" Value="{Binding WizardFooter, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSGridSectionDividerHorizontal" TargetType="Grid">
        <Setter Property="Background" Value="{Binding SectionDivider, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Height" Value="1.5" />
    </Style>

    <Style x:Key="TSGridSectionDividerVertical" TargetType="Grid">
        <Setter Property="Background" Value="{Binding SectionDivider, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Width" Value="1.5" />
    </Style>

    <Style x:Key="TSGridNotification" TargetType="Grid">
        <Setter Property="Background" Value="{Binding IBInfoBarBackground, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSGridSavedTemplate" TargetType="Grid">
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style x:Key="TSGridSavedTemplateError" TargetType="Grid">
        <Setter Property="Background" Value="{Binding IBInfoBarBackground, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="TSGridChangesSummaryDetailFileHeader" TargetType="Grid">
        <Setter Property="Background" Value="{Binding ChangesSummaryDetailFileHeader, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <!--#endregion-->

    <!--#region Hyperlink-->

    <Style x:Key="TSHyperlink" TargetType="Hyperlink">
        <Setter Property="Foreground" Value="{Binding Hyperlink, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextDecorations" Value="{x:Null}" />
        <Setter Property="BaselineAlignment" Value="Bottom" />
        <Setter Property="FocusVisualStyle">
            <Setter.Value>
                <Style>
                    <Setter Property="Control.Template">
                        <Setter.Value>
                            <ControlTemplate>
                                <Rectangle
                                    Margin="2"
                                    SnapsToDevicePixels="true"
                                    Stroke="{Binding Hyperlink, Source={x:Static services:UIStylesService.Instance}}"
                                    StrokeDashArray="1 2"
                                    StrokeThickness="1" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="Foreground" Value="{Binding HyperlinkHover, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Foreground" Value="{Binding HyperlinkDisabled, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--#endregion-->

    <!--#region ScrollViewer-->

    <Style x:Key="ScrollBarButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="2,1,2,1" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border
                        x:Name="border"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="1"
                        SnapsToDevicePixels="true">
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Focusable="False"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Rectangle
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Fill="{TemplateBinding Background}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Rectangle
                        x:Name="rectangle"
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        Margin="3,0,3,0"
                        Fill="{Binding ScrollBarThumbBackground, Source={x:Static services:UIStylesService.Instance}}"
                        SnapsToDevicePixels="True" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="rectangle" Property="Fill" Value="{Binding ScrollBarThumbMouseOverBackground, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsDragging" Value="true">
                            <Setter TargetName="rectangle" Property="Fill" Value="{Binding ScrollBarThumbPressedBackground, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSVerticalScrollBarStyle" TargetType="{x:Type ScrollBar}">
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Width" Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
        <Setter Property="MinWidth" Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}">
                    <Grid x:Name="Bg" SnapsToDevicePixels="true">
                        <Grid.RowDefinitions>
                            <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}" />
                            <RowDefinition Height="0.00001*" />
                            <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}" />
                        </Grid.RowDefinitions>
                        <Border
                            Grid.Row="1"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" />
                        <RepeatButton
                            x:Name="PART_LineUpButton"
                            Command="{x:Static ScrollBar.LineUpCommand}"
                            IsEnabled="{TemplateBinding IsMouseOver}"
                            Style="{StaticResource ScrollBarButton}">
                            <Path
                                x:Name="ArrowTop"
                                Data="M 0,5 L 5,0 L 10,5"
                                Fill="{Binding ScrollBarArrowGlyph, Source={x:Static services:UIStylesService.Instance}}"
                                Stretch="Uniform" />
                        </RepeatButton>
                        <Track
                            x:Name="PART_Track"
                            Grid.Row="1"
                            IsDirectionReversed="true"
                            IsEnabled="{TemplateBinding IsMouseOver}">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource RepeatButtonTransparent}" />
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource RepeatButtonTransparent}" />
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ScrollBarThumbVertical}" />
                            </Track.Thumb>
                        </Track>
                        <RepeatButton
                            x:Name="PART_LineDownButton"
                            Grid.Row="2"
                            Command="{x:Static ScrollBar.LineDownCommand}"
                            IsEnabled="{TemplateBinding IsMouseOver}"
                            Style="{StaticResource ScrollBarButton}">
                            <Path
                                x:Name="ArrowBottom"
                                Data="M 0,0 L 5,5 L 10,0"
                                Fill="{Binding ScrollBarArrowGlyph, Source={x:Static services:UIStylesService.Instance}}"
                                Stretch="Uniform" />
                        </RepeatButton>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineDownButton}" Value="true" />
                                <Condition Binding="{Binding IsPressed, ElementName=PART_LineDownButton}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="ArrowBottom" Property="Fill" Value="{Binding ScrollBarArrowGlyphPressed, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineUpButton}" Value="true" />
                                <Condition Binding="{Binding IsPressed, ElementName=PART_LineUpButton}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="ArrowTop" Property="Fill" Value="{Binding ScrollBarArrowGlyphPressed, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineDownButton}" Value="true" />
                                <Condition Binding="{Binding IsPressed, ElementName=PART_LineDownButton}" Value="false" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="ArrowBottom" Property="Fill" Value="{Binding ScrollBarArrowGlyphMouseOver, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineUpButton}" Value="true" />
                                <Condition Binding="{Binding IsPressed, ElementName=PART_LineUpButton}" Value="false" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="ArrowTop" Property="Fill" Value="{Binding ScrollBarArrowGlyphMouseOver, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="ArrowTop" Property="Fill" Value="{Binding ScrollBarArrowGlyphDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="ArrowBottom" Property="Fill" Value="{Binding ScrollBarArrowGlyphDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="TSScrollViewerControlTemplate" TargetType="{x:Type ScrollViewer}">
        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ScrollContentPresenter
                x:Name="PART_ScrollContentPresenter"
                Grid.Row="0"
                Grid.Column="0"
                Margin="{TemplateBinding Padding}"
                CanContentScroll="{TemplateBinding CanContentScroll}"
                CanHorizontallyScroll="False"
                CanVerticallyScroll="False"
                Content="{TemplateBinding Content}"
                ContentTemplate="{TemplateBinding ContentTemplate}" />
            <ScrollBar
                x:Name="PART_VerticalScrollBar"
                Grid.Row="0"
                Grid.Column="1"
                AutomationProperties.AutomationId="VerticalScrollBar"
                Cursor="Arrow"
                Maximum="{TemplateBinding ScrollableHeight}"
                Minimum="0"
                Style="{DynamicResource TSVerticalScrollBarStyle}"
                ViewportSize="{TemplateBinding ViewportHeight}"
                Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
        </Grid>
    </ControlTemplate>

    <Style x:Key="TSScrollViewer" TargetType="ScrollViewer">
        <Setter Property="Template" Value="{StaticResource TSScrollViewerControlTemplate}" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="PanningMode" Value="VerticalFirst" />
    </Style>

    <!--#endregion-->

    <!--#region ItemsControl-->

    <Style x:Key="TSItemsControl" TargetType="ItemsControl">
        <Setter Property="Focusable" Value="False" />
    </Style>

    <Style
        x:Key="TSItemsControlWithScroll"
        BasedOn="{StaticResource TSItemsControl}"
        TargetType="ItemsControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <ScrollViewer Style="{StaticResource TSScrollViewer}">
                        <ItemsPresenter />
                    </ScrollViewer>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--#endregion-->

    <!--#region TextBox-->

    <SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="#FF7EB4EA" />

    <Style x:Key="TSTextBoxStyle" TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{Binding TextBoxBackground, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderBrush" Value="{Binding TextBoxBorder, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding TextBoxText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AllowDrop" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border
                        x:Name="border"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                        <ScrollViewer
                            x:Name="PART_ContentHost"
                            Focusable="false"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollBarVisibility="Hidden" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource TextBox.MouseOver.Border}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="true">
                <Setter Property="BorderBrush" Value="{Binding TextBoxBorderFocused, Source={x:Static services:UIStylesService.Instance}}" />
                <Setter Property="Background" Value="{Binding TextBoxBackgroundFocused, Source={x:Static services:UIStylesService.Instance}}" />
                <Setter Property="Foreground" Value="{Binding TextBoxTextFocused, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="BorderBrush" Value="{Binding TextBoxBorderDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                <Setter Property="Background" Value="{Binding TextBoxBackgroundDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                <Setter Property="Foreground" Value="{Binding TextBoxTextDisabled, Source={x:Static services:UIStylesService.Instance}}" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsInactiveSelectionHighlightEnabled" Value="true" />
                    <Condition Property="IsSelectionActive" Value="false" />
                </MultiTrigger.Conditions>
                <Setter Property="SelectionBrush" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TSTextBoxPlainText" TargetType="{x:Type TextBox}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AllowDrop" Value="True" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <ScrollViewer
                        x:Name="PART_ContentHost"
                        Focusable="false"
                        HorizontalScrollBarVisibility="Disabled"
                        VerticalScrollBarVisibility="Disabled" />
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false" />
                        <Trigger Property="IsMouseOver" Value="true" />
                        <Trigger Property="IsKeyboardFocused" Value="true" />
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsInactiveSelectionHighlightEnabled" Value="true" />
                    <Condition Property="IsSelectionActive" Value="false" />
                </MultiTrigger.Conditions>
                <Setter Property="SelectionBrush" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}" />
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="TSTextBoxVersion"
        BasedOn="{StaticResource TSTextBoxPlainText}"
        TargetType="TextBox">
        <Setter Property="Foreground" Value="{Binding WindowPanelText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="IsReadOnly" Value="True" />

    </Style>


    <!--  Error Dialog  -->
    <Style
        x:Key="TSTextBlockErrorDialogErrorStackTrace"
        BasedOn="{StaticResource TSTextBoxStyle}"
        TargetType="TextBox">
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.AccessibilityErrorDialogDescriptionTextboxName}" />
    </Style>

    <!--#endregion-->

    <!--#region Ellipse-->

    <Style x:Key="TSEllipseNewItemFileCircle" TargetType="Ellipse">
        <Setter Property="Width" Value="10" />
        <Setter Property="Height" Value="10" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Stroke" Value="{Binding WindowPanel, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <!--#endregion-->

    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <converters:SavedTemplateGridConverter x:Key="SavedTemplateGridConverter" />

    <ItemsPanelTemplate x:Key="TSItemsPanelTemplate">
        <WrapPanel Margin="{StaticResource Margin_M2_LeftRight}" Orientation="Horizontal" />
    </ItemsPanelTemplate>

    <!--#region ListView-->

    <!--  New Project Project type / Framework  -->

    <Style x:Key="TSListViewItemSingleSelection" TargetType="{x:Type ListViewItem}">
        <Setter Property="BorderThickness" Value="{StaticResource CardBorderThickness}" />
        <Setter Property="BorderBrush" Value="{Binding CardBorderDefault, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Background" Value="{Binding CardBackgroundDefault, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Margin" Value="{StaticResource Margin_XS_LeftTopRightBottom}" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Local" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AutomationProperties.Name">
            <Setter.Value>
                <MultiBinding StringFormat="{}{0}, {1}">
                    <Binding Path="Title" />
                    <Binding Path="Summary" />
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListViewItem}">
                    <Border
                        x:Name="border"
                        Margin="{TemplateBinding Margin}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding CardBackgroundSelected, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding CardBorderSelected, Source={x:Static services:UIStylesService.Instance}}" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected}" Value="false" />
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter TargetName="border" Property="BorderBrush" Value="{Binding CardBorderHover, Source={x:Static services:UIStylesService.Instance}}" />
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding CardBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected}" Value="false" />
                                <Condition Binding="{Binding IsFocused, RelativeSource={RelativeSource Self}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter TargetName="border" Property="BorderBrush" Value="{Binding CardBorderHover, Source={x:Static services:UIStylesService.Instance}}" />
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding CardBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="border" Property="Background" Value="{Binding CardBackgroundDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding CardBorderDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnPreviewMouseLeftButtonDown" />
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="IsSelected" Value="True" />
            </Trigger>
            <DataTrigger Binding="{Binding Deprecated}" Value="True">
                <Setter Property="AutomationProperties.Name">
                    <Setter.Value>
                        <MultiBinding StringFormat="{}{0}, {1}, {2}">
                            <Binding Path="Title" />
                            <Binding Source="{x:Static res:Resources.DeprecatedMessage}" />
                            <Binding Path="Summary" />
                        </MultiBinding>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TSListViewSingleSelection" TargetType="{x:Type ListView}">
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalOnly" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ItemsPanel" Value="{StaticResource TSItemsPanelTemplate}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSListViewItemSingleSelection}" />
        <EventSetter Event="PreviewKeyDown" Handler="OnPreviewKeyDown" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListView}">
                    <Border
                        Margin="0"
                        Padding="0"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ScrollViewer
                            Margin="0"
                            Padding="0"
                            Focusable="false"
                            Style="{StaticResource TSScrollViewer}">
                            <ItemsPresenter Margin="0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <!--  TODO Add IsEnabled visual states  -->
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSListViewSingleSelectionWithoutScroll" TargetType="{x:Type ListView}">
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ItemsPanel" Value="{StaticResource TSItemsPanelTemplate}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSListViewItemSingleSelection}" />
        <EventSetter Event="PreviewKeyDown" Handler="OnPreviewKeyDown" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListView}">
                    <Border
                        Margin="0"
                        Padding="0"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ItemsPresenter Margin="0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="TSListViewItemMultipleSelection" TargetType="{x:Type ListViewItem}">
        <Setter Property="BorderThickness" Value="{StaticResource CardBorderThickness}" />
        <Setter Property="BorderBrush" Value="{Binding CardBorderDefault, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Background" Value="{Binding CardBackgroundDefault, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Local" />
        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnPreviewMouseLeftButtonDown" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AutomationProperties.HelpText" Value="{Binding Title, StringFormat={x:Static res:Resources.HelpTextPressEnterToAddItem}}" />
        <Setter Property="AutomationProperties.Name">
            <Setter.Value>
                <MultiBinding StringFormat="{}{0}, {1}">
                    <Binding Path="Title" />
                    <Binding Path="Summary" />
                </MultiBinding>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListViewItem}">
                    <Grid
                        Width="181"
                        Height="219"
                        Margin="0">
                        <Border
                            Margin="14,2,2,14"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1.5"
                            Visibility="{Binding HasMoreThanTwo, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <Border
                            Margin="11,5,5,11"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1.5"
                            Visibility="{Binding HasMoreThanOne, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <Border
                            x:Name="border"
                            Margin="{StaticResource Margin_S_LeftTopRightBottom}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter />
                        </Border>
                        <Grid
                            x:Name="addIcon"
                            Width="30"
                            Height="30"
                            Margin="{StaticResource Margin_M3_LeftTop}"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Visibility="Collapsed">
                            <Ellipse
                                Fill="{Binding CardBackgroundDefault, Source={x:Static services:UIStylesService.Instance}}"
                                Stroke="{Binding CardBorderSelected, Source={x:Static services:UIStylesService.Instance}}"
                                StrokeThickness="1" />
                            <TextBlock Style="{StaticResource TSTextBlockCardMouseOverAddIcon}" />
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{Binding CardBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding CardBorderHover, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="border" Property="Background" Value="{Binding CardBackgroundDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding CardBorderDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                        <DataTrigger Binding="{Binding Disabled}" Value="true">
                            <Setter TargetName="border" Property="BorderBrush" Value="{Binding CardBorderDisabled, Source={x:Static services:UIStylesService.Instance}}" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true" />
                                <Condition Binding="{Binding CanBeAdded}" Value="true" />
                                <Condition Binding="{Binding Disabled}" Value="false" />
                            </MultiDataTrigger.Conditions>
                            <MultiDataTrigger.Setters>
                                <Setter TargetName="addIcon" Property="Visibility" Value="Visible" />
                                <Setter TargetName="border" Property="Background" Value="{Binding CardBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" />
                                <Setter TargetName="border" Property="BorderBrush" Value="{Binding CardBorderHover, Source={x:Static services:UIStylesService.Instance}}" />
                            </MultiDataTrigger.Setters>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="IsSelected" Value="True" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TSListViewMultipleSelection" TargetType="{x:Type ListView}">
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="ScrollViewer.PanningMode" Value="VerticalOnly" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="ItemsPanel" Value="{StaticResource TSItemsPanelTemplate}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSListViewItemMultipleSelection}" />
        <EventSetter Event="PreviewKeyDown" Handler="OnPreviewKeyDown" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListView}">
                    <Border
                        x:Name="Bd"
                        Margin="0"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Focusable="False"
                        SnapsToDevicePixels="true">
                        <ItemsPresenter Margin="0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <!--  TODO Add IsEnabled visual states  -->
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="TSListViewItemSequentialFlow" TargetType="{x:Type ListViewItem}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{Binding TDListBoxText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Margin" Value="{StaticResource Margin_S_Bottom}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Local" />
        <Setter Property="FocusVisualStyle" Value="{Binding FocusVisualStyle, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="AutomationProperties.Name" Value="{Binding Title}" />
        <Setter Property="AutomationProperties.AutomationId" Value="{Binding Path=Index, StringFormat=Step{0:}}" />
        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnPreviewMouseLeftButtonDown" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListViewItem}">
                    <Grid>
                        <Grid
                            x:Name="bd"
                            MinHeight="35"
                            Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                x:Name="text"
                                Grid.Column="0"
                                extensions:TextBlockExtensions.SequentialFlowStep="{Binding}"
                                extensions:TextBlockExtensions.SequentialFlowStepCompleted="{Binding Completed}"
                                extensions:TextBlockExtensions.SequentialFlowStepIndex="{Binding Index}"
                                Foreground="{TemplateBinding Foreground}"
                                Style="{StaticResource TSTextBlockSequentialFlow}" />
                            <TextBlock
                                x:Name="checkMark"
                                Grid.Column="1"
                                Foreground="{TemplateBinding Foreground}"
                                Style="{StaticResource TSTextBlockSequentialFlowCheckMark}"
                                Visibility="{Binding Completed, Converter={StaticResource BoolToVisibilityConverter}}" />
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="true">
                            <Setter TargetName="bd" Property="Background" Value="{Binding TDSelectedItemActive, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="text" Property="Foreground" Value="{Binding TDSelectedItemActiveText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="checkMark" Property="Foreground" Value="{Binding TDSelectedItemActiveText, Source={x:Static services:UIStylesService.Instance}}" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected}" Value="false" />
                                <Condition Binding="{Binding IsFocused, RelativeSource={RelativeSource Self}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="{Binding TDListItemMouseOver, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="text" Property="Foreground" Value="{Binding TDListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="checkMark" Property="Foreground" Value="{Binding TDListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected}" Value="false" />
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="{Binding TDListItemMouseOver, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="text" Property="Foreground" Value="{Binding TDListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="checkMark" Property="Foreground" Value="{Binding TDListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="text" Property="Foreground" Value="{Binding TDListItemDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="checkMark" Property="Foreground" Value="{Binding TDListItemDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSListViewSequentialFlow" TargetType="{x:Type ListView}">
        <Setter Property="Margin" Value="26,20" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="Focusable" Value="False" />
        <EventSetter Event="PreviewKeyDown" Handler="OnPreviewKeyDown" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSListViewItemSequentialFlow}" />
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.AccessibilityNavigationMenu}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListView}">
                    <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="TSListViewItemProjectDetailsTemplates" TargetType="{x:Type ListViewItem}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Local" />
        <Setter Property="AutomationProperties.Name" Value="{Binding Name}" />
        <Setter Property="extensions:FocusExtension.IsFocused" Value="{Binding IsFocused}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListViewItem}">
                    <Grid>
                        <Grid Background="{Binding SavedTemplateBackgroundHover, Source={x:Static services:UIStylesService.Instance}}" Visibility="{Binding IsDragging, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <Grid
                            x:Name="mouseOverGrid"
                            Background="{Binding SavedTemplateBackgroundHover, Source={x:Static services:UIStylesService.Instance}}"
                            Opacity="0" />
                        <Grid Style="{Binding HasErrors, Converter={StaticResource SavedTemplateGridConverter}}" />
                        <Grid Margin="{StaticResource Margin_S_TopBottom}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22" />
                                    <ColumnDefinition Width="20" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="26" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    x:Name="mouseOverDragAndDropTextBlock"
                                    Grid.Column="0"
                                    Opacity="0"
                                    Style="{StaticResource TSTextBlockDragAndDropTemplate}"
                                    Visibility="{Binding IsReorderEnabled, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <controls:ImageEx
                                    Grid.Column="1"
                                    SourcePath="{Binding Icon}"
                                    Style="{StaticResource TSImageExProjectDetails}" />
                                <Grid Grid.Column="2">
                                    <TextBox
                                        Margin="{StaticResource Margin_XS_Left}"
                                        extensions:TextBoxExtensions.IsTextSelected="{Binding IsTextSelected}"
                                        AutomationProperties.Name="{x:Static res:Resources.AccessibilityEditPageItem}"
                                        LostKeyboardFocus="OnLostKeyboardFocus"
                                        Style="{StaticResource TSTextBoxStyle}"
                                        Tag="{Binding LostKeyboardFocusCommand}"
                                        Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        Visibility="{Binding ItemNameEditable, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBox.InputBindings>
                                            <KeyBinding Key="Enter" Command="{Binding SetFocusCommand}" />
                                        </TextBox.InputBindings>
                                    </TextBox>
                                    <TextBlock
                                        Margin="{StaticResource Margin_M_Left}"
                                        Style="{StaticResource TSTextBlockSavedTemplateName}"
                                        Text="{Binding Name, Mode=OneWay}"
                                        Visibility="{Binding ItemNameEditable, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=reverse}" />
                                </Grid>
                                <Button x:Name="mouseOverDeleteTemplateButton" Grid.Column="3">
                                    <Button.Style>
                                        <Style BasedOn="{StaticResource TSButtonDeleteTemplate}" TargetType="Button">
                                            <Setter Property="Opacity" Value="0" />
                                            <Setter Property="Command" Value="{Binding DeleteCommand}" />
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </Grid>
                        <Grid
                            x:Name="dropGridMask"
                            Background="{Binding SavedTemplateBackgroundHover, Source={x:Static services:UIStylesService.Instance}}"
                            Opacity="0.4"
                            Visibility="Collapsed" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="mouseOverGrid" Property="Opacity" Value="1" />
                            <Setter TargetName="mouseOverDragAndDropTextBlock" Property="Opacity" Value="1" />
                            <Setter TargetName="mouseOverDeleteTemplateButton" Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter TargetName="mouseOverGrid" Property="Opacity" Value="1" />
                            <Setter TargetName="mouseOverDragAndDropTextBlock" Property="Opacity" Value="1" />
                            <Setter TargetName="mouseOverDeleteTemplateButton" Property="Opacity" Value="1" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="true">
                            <Setter TargetName="mouseOverGrid" Property="Opacity" Value="1" />
                            <Setter TargetName="mouseOverDragAndDropTextBlock" Property="Opacity" Value="1" />
                            <Setter TargetName="mouseOverDeleteTemplateButton" Property="Opacity" Value="1" />
                        </Trigger>
                        <EventTrigger RoutedEvent="DragEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ObjectAnimationUsingKeyFrames
                                        BeginTime="00:00:00"
                                        Storyboard.TargetName="dropGridMask"
                                        Storyboard.TargetProperty="(UIElement.Visibility)">
                                        <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Visible}" />
                                    </ObjectAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="DragLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ObjectAnimationUsingKeyFrames
                                        BeginTime="00:00:00"
                                        Storyboard.TargetName="dropGridMask"
                                        Storyboard.TargetProperty="(UIElement.Visibility)">
                                        <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Collapsed}" />
                                    </ObjectAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="Drop">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ObjectAnimationUsingKeyFrames
                                        BeginTime="00:00:00"
                                        Storyboard.TargetName="dropGridMask"
                                        Storyboard.TargetProperty="(UIElement.Visibility)">
                                        <DiscreteObjectKeyFrame KeyTime="00:00:00" Value="{x:Static Visibility.Collapsed}" />
                                    </ObjectAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <DataTrigger Binding="{Binding IsReorderEnabled}" Value="true">
                            <Setter Property="AutomationProperties.HelpText" Value="{x:Static res:Resources.ReorderPageHelpText}" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSListViewProjectDetailsTemplates" TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSListViewItemProjectDetailsTemplates}" />
        <Setter Property="SelectionMode" Value="Single" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="AutomationProperties.Name" Value="{x:Static res:Resources.AccessibilityUserSelectionPageList}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListView}">
                    <Border
                        x:Name="Bd"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <Style x:Key="TSListViewItemNewItemFile" TargetType="{x:Type ListViewItem}">
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{Binding ListItemText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Margin" Value="{StaticResource Margin_XS_Bottom}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Local" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="OnPreviewMouseLeftButtonDown" />
        <Setter Property="AutomationProperties.Name" Value="{Binding Subject}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListViewItem}">
                    <Border x:Name="bd" ToolTip="{Binding Subject}">
                        <Grid Margin="{StaticResource Margin_S_LeftTopRightBottom}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Ellipse
                                Grid.Column="0"
                                Margin="{StaticResource Margin_XS_Left}"
                                Fill="{Binding CircleColor, FallbackValue=Red}"
                                Style="{StaticResource TSEllipseNewItemFileCircle}" />
                            <Image
                                Grid.Column="1"
                                Margin="{StaticResource Margin_XS_Left}"
                                Source="{Binding Icon}"
                                Style="{StaticResource TSImageNewItemFileIcon}" />
                            <TextBlock
                                x:Name="fileName"
                                Grid.Column="2"
                                Margin="{StaticResource Margin_XS_LeftRight}"
                                Style="{StaticResource TSTextBlockNewItemFileSubject}"
                                Text="{Binding Subject}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsSelected}" Value="true">
                            <Setter TargetName="bd" Property="Background" Value="{Binding SelectedItemActive, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="fileName" Property="Foreground" Value="{Binding SelectedItemActiveText, Source={x:Static services:UIStylesService.Instance}}" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected}" Value="false" />
                                <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="{Binding ListItemMouseOver, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="fileName" Property="Foreground" Value="{Binding ListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected}" Value="false" />
                                <Condition Binding="{Binding IsFocused, RelativeSource={RelativeSource Self}}" Value="true" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="{Binding ListItemMouseOver, Source={x:Static services:UIStylesService.Instance}}" />
                            <Setter TargetName="fileName" Property="Foreground" Value="{Binding ListItemMouseOverText, Source={x:Static services:UIStylesService.Instance}}" />
                        </MultiDataTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="fileName" Property="Foreground" Value="{Binding ListItemDisabledText, Source={x:Static services:UIStylesService.Instance}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TSListViewNewItemFile" TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{Binding ListItemText, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TSListViewItemNewItemFile}" />
        <Setter Property="SelectionMode" Value="Single" />
        <EventSetter Event="PreviewKeyDown" Handler="OnPreviewKeyDown" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListView}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="true">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--#endregion-->

    <converters:StringVisibilityConverter x:Key="StringVisibilityConverter" />
    <converters:IntegerVisibilityConverter x:Key="IntegerVisibilityConverter" />
    <utilities:BrushToColorConverter x:Key="BrushToColorConverter" />

    <!--#region DataTemplate-->

    <DataTemplate x:Key="TSItemTemplateBasicInfo">
        <Grid Width="162" Height="200">
            <Grid Margin="{StaticResource Margin_M_LeftTopRightBottom}">
                <Grid.RowDefinitions>
                    <!--  Image  -->
                    <RowDefinition Height="55" />
                    <!--  Title  -->
                    <RowDefinition Height="Auto" />
                    <!--  Deprecated message  -->
                    <RowDefinition Height="Auto" />
                    <!--  Description  -->
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <controls:ImageEx
                    Grid.Row="0"
                    SourcePath="{Binding Icon}"
                    Style="{StaticResource TSImageEx}" />
                <TextBlock
                    Grid.Row="1"
                    Style="{StaticResource TSTextBlockCardTitle}"
                    Text="{Binding Title}" />
                <StackPanel
                    Grid.Row="2"
                    Margin="{StaticResource Margin_XS_TopBottom}"
                    theming:ImageThemingUtilities.ImageBackgroundColor="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}, Converter={StaticResource BrushToColorConverter}}"
                    Orientation="Horizontal"
                    Visibility="{Binding Deprecated, Converter={StaticResource BoolToVisibilityConverter}}">
                    <imaging:CrispImage Moniker="{x:Static catalog:KnownMonikers.StatusWarning}" Style="{StaticResource TSCrispImageCardDeprecatedIcon}" />
                    <TextBlock Style="{StaticResource TSTextBlockCardDeprecatedMessage}" Text="{x:Static res:Resources.DeprecatedMessage}" />
                </StackPanel>
                <TextBlock
                    Grid.Row="3"
                    Margin="0,0,0,12"
                    Style="{StaticResource TSTextBlockCardDescription}"
                    Text="{Binding Summary}" />
            </Grid>
            <TextBlock
                VerticalAlignment="Bottom"
                AutomationProperties.HelpText="{Binding Title, StringFormat={x:Static res:Resources.ButtonDetailsHelpText}}"
                AutomationProperties.Name="{x:Static res:Resources.ButtonDetails}">
                <Hyperlink
                    AutomationProperties.HelpText="{Binding Title, StringFormat={x:Static res:Resources.ButtonDetailsHelpText}}"
                    AutomationProperties.Name="{x:Static res:Resources.ButtonDetails}"
                    Command="{Binding DetailsCommand}"
                    Style="{StaticResource TSHyperlink}">
                    <Grid Background="Transparent">
                        <TextBlock
                            Margin="12,8,12,8"
                            Style="{StaticResource TSTextBlockHyperlink}"
                            Text="{x:Static res:Resources.ButtonDetails}" />
                    </Grid>
                </Hyperlink>
            </TextBlock>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="TSItemTemplateCard">
        <Grid>
            <Grid Margin="{StaticResource Margin_M_LeftTopRightBottom}">
                <Grid.RowDefinitions>
                    <!--  Image  -->
                    <RowDefinition Height="55" />
                    <!--  Title  -->
                    <RowDefinition Height="Auto" />
                    <!--  Description  -->
                    <RowDefinition Height="*" />
                    <!--  Detail / Status  -->
                    <RowDefinition Height="16" />
                </Grid.RowDefinitions>
                <Grid
                    Grid.Row="0"
                    Grid.RowSpan="4"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="Transparent" />
                <controls:ImageEx
                    Grid.Row="0"
                    SourcePath="{Binding Icon}"
                    Style="{StaticResource TSImageExWithDisabled}" />
                <TextBlock
                    Grid.Row="1"
                    Style="{StaticResource TSTextBlockCardTitleWithDisabled}"
                    Text="{Binding Title}" />
                <TextBlock
                    Grid.Row="2"
                    Margin="{StaticResource Margin_XS_Top}"
                    Style="{StaticResource TSTextBlockCardDescriptionWithDisabled}"
                    Text="{Binding Summary}" />
                <Grid Grid.Row="3" Margin="0,0,0,-4">
                    <!--  Multiple Instance added feedback  -->
                    <Grid HorizontalAlignment="Right" Visibility="{Binding MultipleInstance, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid
                            Width="18"
                            Height="18"
                            VerticalAlignment="Bottom"
                            Visibility="{Binding Count, Converter={StaticResource IntegerVisibilityConverter}}">
                            <Ellipse Fill="{Binding CardFooterText, Source={x:Static services:UIStylesService.Instance}}" />
                            <TextBlock Style="{StaticResource TSTextBlockCardTemplatesCount}" Text="{Binding Count}" />
                        </Grid>
                    </Grid>
                    <!--  Single Instance added feedback  -->
                    <StackPanel
                        HorizontalAlignment="Right"
                        Orientation="Horizontal"
                        Visibility="{Binding ShowAddedText, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Style="{StaticResource TSTextBlockCardAddedText}" Text="{x:Static res:Resources.TemplateAdded}" />
                        <Grid
                            Width="18"
                            Height="18"
                            Margin="{StaticResource Margin_XS_Left}"
                            VerticalAlignment="Bottom">
                            <Ellipse Fill="{Binding CardFooterText, Source={x:Static services:UIStylesService.Instance}}" />
                            <TextBlock Style="{StaticResource TSTextBlockCardAddedIcon}" />
                        </Grid>
                    </StackPanel>
                </Grid>
            </Grid>
            <TextBlock
                VerticalAlignment="Bottom"
                AutomationProperties.HelpText="{Binding Title, StringFormat={x:Static res:Resources.ButtonDetailsHelpText}}"
                AutomationProperties.Name="{x:Static res:Resources.ButtonDetails}">
                <Hyperlink
                    AutomationProperties.HelpText="{Binding Title, StringFormat={x:Static res:Resources.ButtonDetailsHelpText}}"
                    AutomationProperties.Name="{x:Static res:Resources.ButtonDetails}"
                    Command="{Binding DetailsCommand}"
                    Style="{StaticResource TSHyperlink}">
                    <Grid Background="Transparent">
                        <TextBlock
                            Margin="12,8,12,8"
                            Style="{StaticResource TSTextBlockHyperlink}"
                            Text="{x:Static res:Resources.ButtonDetails}" />
                    </Grid>
                </Hyperlink>
            </TextBlock>
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="TSGroupTemplateCard">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock
                Margin="{StaticResource Margin_L_LeftRight_M_Top}"
                Style="{StaticResource TSTextBlockSubSectionTitle}"
                Text="{Binding Name}"
                Visibility="{Binding Name, Converter={StaticResource StringVisibilityConverter}}" />
            <ListView
                Grid.Row="1"
                AutomationProperties.Name="{Binding AccessibleName}"
                ItemTemplate="{StaticResource TSItemTemplateCard}"
                ItemsSource="{Binding Items}"
                Style="{StaticResource TSListViewMultipleSelection}" />
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="TSNewItemGroupTemplatePageFeature">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock
                Margin="{StaticResource Margin_L_LeftRight_M_Top}"
                Style="{StaticResource TSTextBlockSectionTitle}"
                Text="{Binding Name}"
                Visibility="{Binding Name, Converter={StaticResource StringVisibilityConverter}}" />
            <ListView
                Grid.Row="1"
                AutomationProperties.Name="{Binding AccessibleName}"
                ItemTemplate="{StaticResource TSItemTemplateBasicInfo}"
                ItemsSource="{Binding Items}"
                Style="{StaticResource TSListViewSingleSelectionWithoutScroll}" />
        </Grid>
    </DataTemplate>

    <DataTemplate x:Key="TSItemTemplateDependency">
        <TextBlock AutomationProperties.Name="{Binding Title}">
            <Hyperlink
                AutomationProperties.Name="{Binding Title}"
                Command="{Binding DetailsCommand}"
                Style="{StaticResource TSHyperlink}">
                <Grid Background="Transparent">
                    <TextBlock
                        Margin="4,4,4,4"
                        Style="{StaticResource TSTextBlockHyperlink}"
                        Text="{Binding Title}" />
                </Grid>
            </Hyperlink>
        </TextBlock>
    </DataTemplate>

    <DataTemplate x:Key="TSItemTemplateRequiredSdk">
        <TextBlock
            AutomationProperties.Name="{Binding}"
            Style="{StaticResource TSTextBlockTemplateDetailsPropertyValue}"
            Text="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="TSItemTemplateRequiredDotNetVersion">
        <TextBlock
            AutomationProperties.Name="{Binding}"
            Style="{StaticResource TSTextBlockTemplateDetailsPropertyValue}"
            Text="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="TSItemTemplateRequiredVisualStudioWorkload">
        <TextBlock
            AutomationProperties.Name="{Binding}"
            Style="{StaticResource TSTextBlockTemplateDetailsPropertyValue}"
            Text="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="TSItemTemplateLayout">
        <TextBlock AutomationProperties.Name="{Binding Template.Title}">
            <Hyperlink
                AutomationProperties.Name="{Binding Template.Title}"
                Command="{Binding Template.DetailsCommand}"
                Style="{StaticResource TSHyperlink}">
                <Grid Background="Transparent">
                    <TextBlock
                        Margin="4,4,4,4"
                        Style="{StaticResource TSTextBlockHyperlink}"
                        Text="{Binding Template.Title}" />
                </Grid>
            </Hyperlink>
        </TextBlock>
    </DataTemplate>

    <DataTemplate x:Key="TSItemTemplateLicense">
        <TextBlock AutomationProperties.Name="{Binding Text}">
            <Hyperlink
                AutomationProperties.Name="{Binding Text}"
                Command="{Binding OpenCommand}"
                Style="{StaticResource TSHyperlink}"
                ToolTip="{x:Static res:Resources.ExternalHyperlinkTooltipMessage}">
                <Grid Background="Transparent">
                    <TextBlock
                        Margin="4,4,4,4"
                        Style="{StaticResource TSTextBlockHyperlink}"
                        Text="{Binding Text}" />
                </Grid>
            </Hyperlink>
        </TextBlock>
    </DataTemplate>


    <!--  Changes Summary  -->

    <DataTemplate x:Key="TSNewItemFileGroupHeader">
        <TextBlock Style="{StaticResource TSTextBlockNewItemFileSubject}" Text="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="TSNewItemFileGroup">
        <controls:ExpanderButtonAutomationPeerControl
            Header="{Binding Title}"
            HeaderTemplate="{StaticResource TSNewItemFileGroupHeader}"
            Style="{StaticResource TSExpander}">
            <ListView
                AutomationProperties.Name="{Binding Title}"
                ItemsSource="{Binding Items}"
                Style="{StaticResource TSListViewNewItemFile}" />
        </controls:ExpanderButtonAutomationPeerControl>
    </DataTemplate>

    <DataTemplate x:Key="TSDataTemplateChangesSummaryConflictingFileDetailTemplate">
        <Grid Background="{Binding NewItemFileStatusConflictingFile, Source={x:Static services:UIStylesService.Instance}}" />
    </DataTemplate>

    <DataTemplate x:Key="TSDataTemplateChangesSummaryConflictingStylesFileDetailTemplate">
        <Grid Background="{Binding NewItemFileStatusConflictingStylesFile, Source={x:Static services:UIStylesService.Instance}}" />
    </DataTemplate>

    <DataTemplate x:Key="TSDataTemplateChangesSummaryCodeViewer">
        <controls:CodeViewer Item="{Binding}" />
    </DataTemplate>

    <DataTemplate x:Key="TSDataTemplateChangesSummaryImageViewer">
        <Image extensions:ImageExtensions.DisposableSource="{Binding TempFile}" Style="{StaticResource TSImageNewItemImageFiles}" />
    </DataTemplate>

    <templateSelector:FileExtensionTemplateSelector
        x:Key="fileExtensionTemplateSelector"
        CodeFileTemplate="{StaticResource TSDataTemplateChangesSummaryCodeViewer}"
        ImageFileTemplate="{StaticResource TSDataTemplateChangesSummaryImageViewer}" />

    <templateSelector:FileStatusTemplateSelector x:Key="FileStatusTemplateSelector">
        <templateSelector:FileStatusTemplateSelector.ConflictingFileTemplate>
            <DataTemplate>
                <TextBlock Style="{StaticResource TSTextBlockFileStatusDescription}" Text="{x:Static res:Resources.ChangesSummaryGroupConflictingFilesDescription}" />
            </DataTemplate>
        </templateSelector:FileStatusTemplateSelector.ConflictingFileTemplate>
        <templateSelector:FileStatusTemplateSelector.ConflictingStylesFileTemplate>
            <DataTemplate>
                <TextBlock Style="{StaticResource TSTextBlockFileStatusDescription}" Text="{x:Static res:Resources.ChangesSummaryGroupConflictingStylesFilesDescription}" />
            </DataTemplate>
        </templateSelector:FileStatusTemplateSelector.ConflictingStylesFileTemplate>
        <templateSelector:FileStatusTemplateSelector.ModifiedFileTemplate>
            <DataTemplate>
                <TextBlock Style="{StaticResource TSTextBlockFileStatusDescription}" Text="{x:Static res:Resources.ChangesSummaryGroupModifiedFilesDescription}" />
            </DataTemplate>
        </templateSelector:FileStatusTemplateSelector.ModifiedFileTemplate>
        <templateSelector:FileStatusTemplateSelector.NewFileTemplate>
            <DataTemplate>
                <TextBlock Style="{StaticResource TSTextBlockFileStatusDescription}" Text="{x:Static res:Resources.ChangesSummaryGroupNewFilesDescription}" />
            </DataTemplate>
        </templateSelector:FileStatusTemplateSelector.NewFileTemplate>
        <templateSelector:FileStatusTemplateSelector.UnchangedFileTemplate>
            <DataTemplate>
                <TextBlock Style="{StaticResource TSTextBlockFileStatusDescription}" Text="{x:Static res:Resources.ChangesSummaryGroupUnchangedFilesDescription}" />
            </DataTemplate>
        </templateSelector:FileStatusTemplateSelector.UnchangedFileTemplate>
        <templateSelector:FileStatusTemplateSelector.WarningFileTemplate>
            <DataTemplate>
                <StackPanel>
                    <TextBlock Style="{StaticResource TSTextBlockFileStatusDescription}" Text="{x:Static res:Resources.ChangesSummaryGroupWarningFilesDescription}" />
                    <TextBlock
                        Margin="{StaticResource Margin_XS_Top}"
                        Style="{StaticResource TSTextBlockFileStatusDescription}"
                        Text="{Binding Description}" />
                </StackPanel>
            </DataTemplate>
        </templateSelector:FileStatusTemplateSelector.WarningFileTemplate>
    </templateSelector:FileStatusTemplateSelector>

    <DataTemplate x:Key="TSDataTemplateChangesSummaryDetailTemplate">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ContentPresenter
                    Grid.Column="0"
                    Margin="{StaticResource Margin_M_LeftTopRightBottom}"
                    ContentTemplateSelector="{StaticResource FileStatusTemplateSelector}" />
                <TextBlock Grid.Column="1" AutomationProperties.Name="{x:Static res:Resources.ChangesSummaryMoreInfoLink}">
                    <Hyperlink
                        AutomationProperties.Name="{x:Static res:Resources.ChangesSummaryMoreInfoLink}"
                        Command="{Binding MoreDetailsCommand}"
                        Style="{StaticResource TSHyperlink}"
                        ToolTip="{x:Static res:Resources.ExternalHyperlinkTooltipMessage}">
                        <Grid Background="Transparent">
                            <TextBlock
                                Margin="{StaticResource Margin_M_LeftTopRightBottom}"
                                Style="{StaticResource TSTextBlockHyperlink}"
                                Text="{x:Static res:Resources.ChangesSummaryMoreInfoLink}" />
                        </Grid>
                    </Hyperlink>
                </TextBlock>
            </Grid>
            <Grid Grid.Row="1" Style="{StaticResource TSGridChangesSummaryDetailFileHeader}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image
                    Grid.Column="0"
                    Margin="{StaticResource Margin_M_Left_S_TopBottom}"
                    Source="{Binding Icon}"
                    Style="{StaticResource TSImageNewItemFileIcon}" />
                <TextBlock
                    Grid.Column="1"
                    Margin="{StaticResource Margin_M_Left_S_TopBottom}"
                    Style="{StaticResource TSTextBlockChangesSummaryDetailFileHeader}"
                    Text="{Binding Subject}" />
            </Grid>
            <ContentPresenter
                Grid.Row="2"
                Content="{Binding}"
                ContentTemplateSelector="{StaticResource fileExtensionTemplateSelector}"
                Focusable="False" />
        </Grid>
    </DataTemplate>


    <DataTemplate x:Key="TSItemTemplateBreakingChanges">
        <Grid Margin="{StaticResource Margin_M_Top}">
            <BulletDecorator>
                <BulletDecorator.Bullet>
                    <TextBlock Style="{StaticResource TSTextBlockBullet}" />
                </BulletDecorator.Bullet>
                <TextBlock Margin="{StaticResource Margin_XS_Left}" TextWrapping="Wrap">
                    <Run Text="{Binding Message}" />
                    <Hyperlink
                        AutomationProperties.Name="{Binding HyperLinkMessage}"
                        Command="{Binding OpenLinkCommand}"
                        Style="{StaticResource TSHyperlink}"
                        ToolTip="{x:Static res:Resources.ExternalHyperlinkTooltipMessage}">
                        <Grid Background="Transparent">
                            <TextBlock Style="{StaticResource TSTextBlockHyperlink}" Text="{Binding HyperLinkMessage}" />
                        </Grid>
                    </Hyperlink>
                </TextBlock>
            </BulletDecorator>
        </Grid>
    </DataTemplate>

    <!--#endregion-->

    <!--#region MarkdownBlock-->

    <Style x:Key="DocumentStyle" TargetType="FlowDocument">
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="TextAlignment" Value="Left" />
        <Setter Property="PagePadding" Value="0" />
        <Setter Property="FontStyle" Value="Normal" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="FontSize" Value="{Binding Environment122PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding TemplateInfoPageDescription, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="BaseMarkdownText" TargetType="Paragraph">
        <Setter Property="Foreground" Value="{Binding TemplateInfoPageDescription, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontFamily" Value="{Binding EnvironmentFontFamily, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontSize" Value="{Binding Environment122PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style
        x:Key="H1Style"
        BasedOn="{StaticResource BaseMarkdownText}"
        TargetType="Paragraph">
        <Setter Property="FontSize" Value="{Binding Environment155PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style
        x:Key="H2Style"
        BasedOn="{StaticResource BaseMarkdownText}"
        TargetType="Paragraph">
        <Setter Property="FontSize" Value="{Binding Environment133PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style
        x:Key="H3Style"
        BasedOn="{StaticResource BaseMarkdownText}"
        TargetType="Paragraph">
        <Setter Property="FontSize" Value="{Binding Environment133PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style
        x:Key="H4Style"
        BasedOn="{StaticResource BaseMarkdownText}"
        TargetType="Paragraph">
        <Setter Property="FontSize" Value="{Binding Environment122PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <Style x:Key="ImageStyle" TargetType="Image">
        <Setter Property="RenderOptions.BitmapScalingMode" Value="NearestNeighbor" />
        <Style.Triggers>
            <Trigger Property="Tag" Value="imageright">
                <Setter Property="Margin" Value="20,0,0,0" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SeparatorStyle" TargetType="Line">
        <Setter Property="X2" Value="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=FlowDocumentScrollViewer}}" />
        <Setter Property="Stroke" Value="{Binding SectionDivider, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="StrokeThickness" Value="2" />
    </Style>

    <!--  Notification markdown  -->
    <Style x:Key="TSNotificationMarkdownStyle" TargetType="controls:NotificationMarkdownBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="Margin" Value="{StaticResource Margin_M_LeftTopRightBottom}" />
    </Style>

    <Style
        x:Key="DocumentNotificationStyle"
        BasedOn="{StaticResource DocumentStyle}"
        TargetType="FlowDocument">
        <Setter Property="FontSize" Value="{Binding Environment100PercentFontSize, Source={x:Static services:UIStylesService.Instance}}" />
        <Setter Property="Foreground" Value="{Binding IBInfoBarBackgroundText, Source={x:Static services:UIStylesService.Instance}}" />
    </Style>

    <!--#endregion-->

</ResourceDictionary>
